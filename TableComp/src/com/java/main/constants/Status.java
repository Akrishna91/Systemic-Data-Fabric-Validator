package com.java.main.constants;

public enum Status {
	
	/**
	 * if everything's good
	 */
	OK,
	
	/**
	 * if there is row mismatch
	 */
	ROW_MISMATCH,
	/**
	 * if there is col mismatch
	 */
	COL_MISMATCH,
	
	/**
	 * if both row and col mismatch
	 */
	ROW_COL_MISMATCH;
	
}
